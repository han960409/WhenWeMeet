spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/whenwemeet
    username: postgres
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true

    show-sql: true

server:
  port: 8080